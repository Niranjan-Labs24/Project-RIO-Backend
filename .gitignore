node_modules/
dist/
.env
.env.*
!.env.example
*.log
coverage/
prisma/*.db
src/generated/

# Uploaded evidence files — local-disk storage for this phase (see
# evidence.storage.service.ts); never belongs in the repo.
storage/

# Claude Code / AI assistant local files and dependency docs — never commit
.claude/
**/.claude/
.claude*
.superpowers/
**/.superpowers/
.mcp.json
CLAUDE*.md
**/CLAUDE*.md
claude*.md
**/claude*.md
# TLS dev certs / private keys — never commit keys
certs/

# RIO-FR-008 — cached geoBoundaries downloads for scripts/build-geo-coordinates.mjs.
# The derived governorate-coordinates.json IS committed; these raw files are not.
prisma/data/.geoboundaries/
